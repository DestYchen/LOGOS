version: "3.9"

services:
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: supplyhub
      POSTGRES_USER: supplyhub
      POSTGRES_PASSWORD: supplyhub
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U supplyhub"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    restart: unless-stopped
    depends_on:
      init-db:
        condition: service_completed_successfully
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      SUPPLYHUB_DATABASE_URL: postgresql+asyncpg://supplyhub:supplyhub@db:5432/supplyhub
      SUPPLYHUB_REDIS_URL: redis://redis:6379/0
      SUPPLYHUB_JSON_FILLER_ENDPOINT: ${SUPPLYHUB_JSON_FILLER_ENDPOINT-http://openclaw-json-filler:8013/v1/fill}
      SUPPLYHUB_DOC_CLASSIFIER_ENDPOINT: ${SUPPLYHUB_DOC_CLASSIFIER_ENDPOINT-http://openclaw-doc-classifier:8012/v1/classify}
      SUPPLYHUB_DOC_ASSEMBLER_ENDPOINT: ${SUPPLYHUB_DOC_ASSEMBLER_ENDPOINT-http://openclaw-doc-assembler:8014/v1/assemble}
      SUPPLYHUB_DOC_ASSEMBLER_TIMEOUT: ${SUPPLYHUB_DOC_ASSEMBLER_TIMEOUT:-60}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_ENABLED: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_ENABLED:-1}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_TIMEOUT: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_TIMEOUT:-180}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_DPI: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_DPI:-300}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_PIXELS: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_PIXELS:-4000000}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_COMPLETION_TOKENS: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_COMPLETION_TOKENS:-24000}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_BASE_URL: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_BASE_URL:-http://host.docker.internal:18789/v1/responses}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_API_KEY: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_API_KEY:-${OPENCLAW_API_TOKEN:-}}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_MODEL: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_MODEL:-${OPENCLAW_MODEL:-openclaw/default}}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_TIMEOUT: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_TIMEOUT:-120}
      SUPPLYHUB_LOCAL_ARCHIVE_MODE: "1"
      SUPPLYHUB_LOCAL_ARCHIVE_DIR: /app/local_archive
      SUPPLYHUB_REMOTE_JSON_FILLER_PROVIDER: http
      SUPPLYHUB_REMOTE_JSON_FILLER_ENDPOINT: http://remote-json-filler:8004/v1/fill
      SUPPLYHUB_CELERY_BROKER_URL: redis://redis:6379/1
      SUPPLYHUB_CELERY_RESULT_BACKEND: redis://redis:6379/2
      SUPPLYHUB_BASE_DIR: /data/storage
      SUPPLYHUB_OCR_ENDPOINT: ${SUPPLYHUB_OCR_ENDPOINT-http://openclaw-ocr:8011/v1/ocr}
      SUPPLYHUB_USE_STUB_SERVICES: "0"
      # vLLM-only OCR adapter configuration
      DOTS_OCR_REPO: /vendor/dots.ocr
      DOTS_OCR_VLLM_HOST: host.docker.internal
      DOTS_OCR_VLLM_PORT: "8010"
      DOTS_OCR_VLLM_BASE: http://host.docker.internal:8010/v1
      DOTS_OCR_PROMPT_OVERRIDE: |-
        Please output the layout information from the PDF image, including each layout element's bbox, its category, and the corresponding text content within the bbox. 1. Bbox format: [x1, y1, x2, y2] 2. Layout Categories: The possible categories are ['Caption', 'Footnote', 'Formula', 'List-item', 'Page-footer', 'Page-header', 'Section-header', 'Table', 'Text', 'Title']. 3. Text Extraction & Formatting Rules: - Formula: Format its text as LaTeX. - Table: Format its text as HTML. - All Others (Text, Title, etc.): Format their text as Markdown. - When an element contains textual content that resembles paragraphs, titles, captions, or tables, prefer the closest text-oriented category instead of using picture-like labels. 4. Constraints: - The output text must be the original text from the image, with no translation. - All layout elements must be sorted according to human reading order. 5. Final Output: Output a single JSON array (no prose), where each element is an object with keys: bbox (as [x1, y1, x2, y2]), category (one of the listed categories), and text (string). Do not include any keys other than bbox, category, and text. Do not include any explanations or extra text outside the JSON array.
      DOTS_OCR_DEBUG_LOG_PROMPT: "1"
      # OCR prompt + debug controls
      DOTS_OCR_DISABLE_PICTURE_DATATYPE: "1"
      DOTS_OCR_DEBUG_DUMP_RAW: "1"
      DOTS_OCR_DEBUG_DIR: /data/storage/dotsocr_debug
      DOTS_OCR_DEBUG_LOG_RAW: "1"
      DOTS_OCR_DEBUG_LOG_CHUNK: "8000"
    volumes:
      - storage:/data/storage
      # Mount local dots.ocr repository for utils (no HF/torch required)
      - ../dots/dots.ocr:/vendor/dots.ocr:ro
      # Mount source code so container uses latest backend without rebuild
      - ./app:/app/app:ro
      # Local archive root (host project folder)
      - ./local_archive:/app/local_archive
    ports:
      - "8000:8000"

  worker:
    build: .
    restart: unless-stopped
    depends_on:
      init-db:
        condition: service_completed_successfully
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      SUPPLYHUB_DATABASE_URL: postgresql+asyncpg://supplyhub:supplyhub@db:5432/supplyhub
      SUPPLYHUB_JSON_FILLER_ENDPOINT: ${SUPPLYHUB_JSON_FILLER_ENDPOINT-http://openclaw-json-filler:8013/v1/fill}
      SUPPLYHUB_REDIS_URL: redis://redis:6379/0
      SUPPLYHUB_DOC_CLASSIFIER_ENDPOINT: ${SUPPLYHUB_DOC_CLASSIFIER_ENDPOINT-http://openclaw-doc-classifier:8012/v1/classify}
      SUPPLYHUB_DOC_ASSEMBLER_ENDPOINT: ${SUPPLYHUB_DOC_ASSEMBLER_ENDPOINT-http://openclaw-doc-assembler:8014/v1/assemble}
      SUPPLYHUB_DOC_ASSEMBLER_TIMEOUT: ${SUPPLYHUB_DOC_ASSEMBLER_TIMEOUT:-60}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_ENABLED: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_ENABLED:-1}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_TIMEOUT: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_TIMEOUT:-180}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_DPI: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_DPI:-300}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_PIXELS: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_PIXELS:-4000000}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_COMPLETION_TOKENS: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_DOTS_MAX_COMPLETION_TOKENS:-24000}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_BASE_URL: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_BASE_URL:-http://host.docker.internal:18789/v1/responses}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_API_KEY: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_API_KEY:-${OPENCLAW_API_TOKEN:-}}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_MODEL: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_MODEL:-${OPENCLAW_MODEL:-openclaw/default}}
      SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_TIMEOUT: ${SUPPLYHUB_FIELD_BBOX_GROUNDING_OPENCLAW_TIMEOUT:-120}
      SUPPLYHUB_LOCAL_ARCHIVE_MODE: "1"
      SUPPLYHUB_LOCAL_ARCHIVE_DIR: /app/local_archive
      SUPPLYHUB_REMOTE_JSON_FILLER_PROVIDER: http
      SUPPLYHUB_REMOTE_JSON_FILLER_ENDPOINT: http://remote-json-filler:8004/v1/fill
      SUPPLYHUB_CELERY_BROKER_URL: redis://redis:6379/1
      SUPPLYHUB_CELERY_RESULT_BACKEND: redis://redis:6379/2
      SUPPLYHUB_BASE_DIR: /data/storage
      SUPPLYHUB_OCR_ENDPOINT: ${SUPPLYHUB_OCR_ENDPOINT-http://openclaw-ocr:8011/v1/ocr}
      SUPPLYHUB_USE_STUB_SERVICES: "0"
      # vLLM-only OCR adapter configuration
      DOTS_OCR_REPO: /vendor/dots.ocr
      DOTS_OCR_VLLM_HOST: host.docker.internal
      DOTS_OCR_VLLM_PORT: "8010"
      DOTS_OCR_VLLM_BASE: http://host.docker.internal:8010/v1
      DOTS_OCR_PROMPT_OVERRIDE: |-
        Please output the layout information from the PDF image, including each layout element's bbox, its category, and the corresponding text content within the bbox. 1. Bbox format: [x1, y1, x2, y2] 2. Layout Categories: The possible categories are ['Caption', 'Footnote', 'Formula', 'List-item', 'Page-footer', 'Page-header', 'Section-header', 'Table', 'Text', 'Title']. 3. Text Extraction & Formatting Rules: - Formula: Format its text as LaTeX. - Table: Format its text as HTML. - All Others (Text, Title, etc.): Format their text as Markdown. - When an element contains textual content that resembles paragraphs, titles, captions, or tables, prefer the closest text-oriented category instead of using picture-like labels. 4. Constraints: - The output text must be the original text from the image, with no translation. - All layout elements must be sorted according to human reading order. 5. Final Output: Output a single JSON array (no prose), where each element is an object with keys: bbox (as [x1, y1, x2, y2]), category (one of the listed categories), and text (string). Do not include any keys other than bbox, category, and text. Do not include any explanations or extra text outside the JSON array.
      DOTS_OCR_DEBUG_LOG_PROMPT: "1"
      # OCR prompt + debug controls
      DOTS_OCR_DISABLE_PICTURE_DATATYPE: "1"
      DOTS_OCR_DEBUG_DUMP_RAW: "1"
      DOTS_OCR_DEBUG_DIR: /data/storage/dotsocr_debug
      DOTS_OCR_DEBUG_LOG_RAW: "1"
      DOTS_OCR_DEBUG_LOG_CHUNK: "8000"
    volumes:
      - storage:/data/storage
      - ../dots/dots.ocr:/vendor/dots.ocr:ro
      # Mount source code for worker to use latest backend without rebuild
      - ./app:/app/app:ro
      # Local archive root (host project folder)
      - ./local_archive:/app/local_archive
    command: [
      "celery",
      "-A",
      "app.workers.celery_app.celery_app",
      "worker",
      "--loglevel=info"
    ]

  beat:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      SUPPLYHUB_DATABASE_URL: postgresql+asyncpg://supplyhub:supplyhub@db:5432/supplyhub
      SUPPLYHUB_CELERY_BROKER_URL: redis://redis:6379/1
      SUPPLYHUB_CELERY_RESULT_BACKEND: redis://redis:6379/2
    volumes:
      - ./app:/app/app:ro
    command: [
      "celery",
      "-A",
      "app.workers.celery_app.celery_app",
      "beat",
      "--loglevel=info"
    ]

  doc-classifier:
    build: .
    restart: unless-stopped
    command: [
      "uvicorn",
      "app.mock_services.chatgpt_doc_classifier:app",
      "--host",
      "0.0.0.0",
      "--port",
      "8003"
    ]
    volumes:
      - ./app:/app/app:ro
    environment:
      DOC_CLASSIFIER_BASE_URL: ${DOC_CLASSIFIER_BASE_URL:-http://10.0.0.247:1234/v1}
      DOC_CLASSIFIER_API_KEY: ${DOC_CLASSIFIER_API_KEY:-}
      DOC_CLASSIFIER_MODEL: ${DOC_CLASSIFIER_MODEL:-openai/gpt-oss-20b}
      DOC_CLASSIFIER_MAX_TEXT_CHARS: ${DOC_CLASSIFIER_MAX_TEXT_CHARS:-20000}
    ports:
      - "8003:8003"

  remote-json-filler:
    build: .
    restart: unless-stopped
    command: [
      "uvicorn",
      "app.mock_services.openrouter_json_filler:app",
      "--host",
      "0.0.0.0",
      "--port",
      "8004"
    ]
    environment:
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_BASE_URL: ${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}
      OPENROUTER_MODEL_MAIN: ${OPENROUTER_MODEL_MAIN:-openai/gpt-oss-120b}
      OPENROUTER_MODEL_PRODUCTS: ${OPENROUTER_MODEL_PRODUCTS:-openai/gpt-oss-120b}
      OPENROUTER_HTTP_REFERER: ${OPENROUTER_HTTP_REFERER:-http://localhost}
      OPENROUTER_X_TITLE: ${OPENROUTER_X_TITLE:-SupplyHub JSON Filler}
      SUPPLYHUB_LOCAL_ARCHIVE_MODE: "1"
      SUPPLYHUB_LOCAL_ARCHIVE_DIR: /app/local_archive
    volumes:
      - ./app:/app/app:ro
      # Local archive root (host project folder)
      - ./local_archive:/app/local_archive
    ports:
      - "8004:8004"

  openclaw-ocr:
    build: .
    restart: unless-stopped
    command: [
      "uvicorn",
      "app.mock_services.openclaw_ocr:app",
      "--host",
      "0.0.0.0",
      "--port",
      "8011"
    ]
    environment:
      OPENCLAW_API_TOKEN: ${OPENCLAW_API_TOKEN:-}
      OPENCLAW_BASE_URL: ${OPENCLAW_DOCKER_BASE_URL:-http://host.docker.internal:18789/v1/responses}
      OPENCLAW_MODEL: ${OPENCLAW_MODEL:-openclaw/default}
      OPENCLAW_TIMEOUT: ${OPENCLAW_TIMEOUT:-300}
      OPENCLAW_OCR_PDF_DPI: ${OPENCLAW_OCR_PDF_DPI:-300}
      OPENCLAW_OCR_MAX_PIXELS: ${OPENCLAW_OCR_MAX_PIXELS:-4000000}
    volumes:
      - ./app:/app/app:ro
    ports:
      - "8011:8011"

  openclaw-doc-classifier:
    build: .
    restart: unless-stopped
    command: [
      "uvicorn",
      "app.mock_services.openclaw_doc_classifier:app",
      "--host",
      "0.0.0.0",
      "--port",
      "8012"
    ]
    environment:
      OPENCLAW_API_TOKEN: ${OPENCLAW_API_TOKEN:-}
      OPENCLAW_CLASSIFIER_BASE_URL: ${OPENCLAW_CLASSIFIER_DOCKER_BASE_URL:-http://host.docker.internal:18789/v1/responses}
      OPENCLAW_CLASSIFIER_MODEL: ${OPENCLAW_CLASSIFIER_MODEL:-${OPENCLAW_MODEL:-openclaw/default}}
      OPENCLAW_CLASSIFIER_TIMEOUT: ${OPENCLAW_CLASSIFIER_TIMEOUT:-120}
      OPENCLAW_CLASSIFIER_MAX_TEXT_CHARS: ${OPENCLAW_CLASSIFIER_MAX_TEXT_CHARS:-20000}
    volumes:
      - ./app:/app/app:ro
    ports:
      - "8012:8012"

  openclaw-json-filler:
    build: .
    restart: unless-stopped
    command: [
      "uvicorn",
      "app.mock_services.openclaw_json_filler:app",
      "--host",
      "0.0.0.0",
      "--port",
      "8013"
    ]
    environment:
      OPENCLAW_API_TOKEN: ${OPENCLAW_API_TOKEN:-}
      OPENCLAW_FILLER_BASE_URL: ${OPENCLAW_FILLER_DOCKER_BASE_URL:-http://host.docker.internal:18789/v1/responses}
      OPENCLAW_FILLER_MODEL: ${OPENCLAW_FILLER_MODEL:-${OPENCLAW_MODEL:-openclaw/default}}
      OPENCLAW_FILLER_TIMEOUT: ${OPENCLAW_FILLER_TIMEOUT:-180}
      OPENCLAW_FILLER_MAX_TOKENS_CHARS: ${OPENCLAW_FILLER_MAX_TOKENS_CHARS:-200000}
      OPENCLAW_FILLER_MAX_TEXT_CHARS: ${OPENCLAW_FILLER_MAX_TEXT_CHARS:-40000}
      OPENCLAW_FILLER_MAX_TEMPLATE_CHARS: ${OPENCLAW_FILLER_MAX_TEMPLATE_CHARS:-100000}
    volumes:
      - ./app:/app/app:ro
    ports:
      - "8013:8013"

  openclaw-doc-assembler:
    build: .
    restart: unless-stopped
    command: [
      "uvicorn",
      "app.mock_services.openclaw_doc_assembler:app",
      "--host",
      "0.0.0.0",
      "--port",
      "8014"
    ]
    environment:
      OPENCLAW_API_TOKEN: ${OPENCLAW_API_TOKEN:-}
      OPENCLAW_ASSEMBLER_BASE_URL: ${OPENCLAW_ASSEMBLER_DOCKER_BASE_URL:-http://host.docker.internal:18789/v1/responses}
      OPENCLAW_ASSEMBLER_MODEL: ${OPENCLAW_ASSEMBLER_MODEL:-${OPENCLAW_MODEL:-openclaw/default}}
      OPENCLAW_ASSEMBLER_TIMEOUT: ${OPENCLAW_ASSEMBLER_TIMEOUT:-120}
      OPENCLAW_ASSEMBLER_MAX_PAGE_CHARS: ${OPENCLAW_ASSEMBLER_MAX_PAGE_CHARS:-5000}
    volumes:
      - ./app:/app/app:ro
    ports:
      - "8014:8014"

  init-db:
    build: .
    depends_on:
      db:
        condition: service_healthy
    environment:
      SUPPLYHUB_DATABASE_URL: postgresql+asyncpg://supplyhub:supplyhub@db:5432/supplyhub
    command: ["python", "-m", "app.scripts.init_db"]
    restart: "no"

volumes:
  postgres_data:
  storage:
