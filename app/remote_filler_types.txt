# One DocumentType per line for remote JSON filler routing.
# Example:
# PRICE_LIST_1
# CMR
# Keep this file empty to route every document type through the local
# SUPPLYHUB_JSON_FILLER_ENDPOINT, which defaults to openclaw-json-filler.
